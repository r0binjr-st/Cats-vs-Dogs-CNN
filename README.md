# Cats-vs-Dogs-CNN
Binary image classifier using PyTorch.  ## Pipeline - load data from Google Drive   - preprocess images (resize, normalize)   - train CNN   - evaluate (loss, accuracy)  ## Model Conv → ReLU → Pool ×3 → FC  ## Result Accuracy ~80–90%  ## Run Execute all cells in order.
